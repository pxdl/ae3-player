{
  "name": "ae3-player",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Browser player for the Ape Escape 3 soundtrack -- client-side extraction from the user's own disc, WASM synth in an AudioWorklet.",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/gen-sw.mjs",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "sync-sdk": "sh scripts/sync-sdk.sh"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vite": "^7.0.0"
  }
}

{
  "name": "ae3-player",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Client-side Ape Escape 3 music, sound, and FMV player with local extraction, conversion, and offline caching.",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/gen-sw.mjs",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test:scheduler": "node --experimental-strip-types --test tests/movie-scheduler.test.ts",
    "sync-sdk": "sh scripts/sync-sdk.sh"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vite": "^7.0.0"
  },
  "dependencies": {
    "mediabunny": "^1.50.9"
  }
}
